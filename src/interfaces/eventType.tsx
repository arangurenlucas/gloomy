export interface eventType {
  profilePhoto: string;
  organizer: string;
  img: string;
  title: string;
  category: string;
  description: string;
  date: string;
}
export interface eventHeaderType {
  profilePhoto: string;
  organizer: string;
}

export interface eventInfoType {
  title: string;
  category: string;
  description: string;
  date: string;
}
